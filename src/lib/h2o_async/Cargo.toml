[package]
edition = "2021"
name = "solvent-async"
version = "0.1.0"

[dependencies]
# Local crates
solvent = {path = "../h2o_rs"}
solvent-std = {path = "../libr"}
# External crates
futures = {version = "0.3", default-features = false, features = ["alloc"]}
log = "0.4"
