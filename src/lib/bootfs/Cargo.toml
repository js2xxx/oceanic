[package]
edition = "2021"
name = "bootfs"
version = "0.1.0"

[features]
gen = ["dep:anyhow"]

[dependencies]
anyhow = {version = "1.0", optional = true}
either = {version = "1.6", default-features = false}
plain = "0.2"
static_assertions = "1.1"
