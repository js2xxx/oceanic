[package]
edition = "2021"
name = "rpc"
version = "0.1.0"

[features]
call = ["solvent/call"]
default = ["solvent/default"]

[dependencies]
# Local crates
solvent = {path = "../h2o_rs", default-features = false}
solvent-async = {path = "../h2o_async"}
