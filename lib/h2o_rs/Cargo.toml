[package]
edition = "2021"
license = "MIT OR Apache-2.0"
name = "solvent"
version = "0.1.0"

[dependencies]
# Local crates
sv-call = {path = "../../h2o/libs/syscall"}
# External crates
cfg-if = "1.0"
