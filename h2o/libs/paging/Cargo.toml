[package]
authors = ["Js2xxx"]
edition = "2021"
license = "MIT OR Apache-2.0"
name = "paging"
version = "0.1.0"

[features]
default = []

[dependencies]
bitflags = "1.3"
cfg-if = "1.0"
log = "0.4"
num-traits = {version = "0.2", default-features = false}
static_assertions = "1.1"
